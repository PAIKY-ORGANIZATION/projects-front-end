#! This is assuming you are running a Redis instance and setting that Redis instance to run in a custom network called paikynet.



REDIS_HOST=REDIS_LOCAL #! Coming from the local paikynetwork
REDIS_PORT=6379